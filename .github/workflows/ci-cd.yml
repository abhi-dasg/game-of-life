name: CI/CD Pipeline

on:
  push:
    branches: [ main, dev ]
  pull_request:
    branches: [ main, dev ]
  workflow_dispatch:

jobs:
  build-test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '10.0.x'
    
    - name: Restore dependencies
      run: dotnet restore
      working-directory: ./src
    
    - name: Build solution
      run: dotnet build --no-restore --configuration Release
      working-directory: ./src
    
    - name: Run Tests with Coverage
      run: dotnet test --no-build --configuration Release -- --coverage --coverage-output-format cobertura --coverage-output ./TestResults --report-trx
      working-directory: ./src
    
    - name: List test output files (debug)
      if: always()
      run: |
        echo "Searching for test output files..."
        find . -type f \( -name "*.cobertura.xml" -o -name "coverage.cobertura.xml" -o -name "*.trx" \) | head -30
      working-directory: ./src
    
    - name: Code Coverage Report
      uses: irongut/CodeCoverageSummary@v1.3.0
      with:
        filename: 'src/**/TestResults/*/coverage.cobertura.xml'
        badge: true
        fail_below_min: false
        format: markdown
        hide_branch_rate: false
        hide_complexity: false
        indicators: true
        output: both
        thresholds: '60 80'
    
    - name: Add Coverage PR Comment
      uses: marocchino/sticky-pull-request-comment@v2
      if: github.event_name == 'pull_request'
      with:
        recreate: true
        path: code-coverage-results.md
    
    - name: Upload Test Results
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: test-results
        path: |
          src/**/TestResults/**/*.trx
        retention-days: 30
        if-no-files-found: warn
    
    - name: Upload Coverage Reports
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: coverage-reports
        path: |
          src/**/TestResults/*/coverage.cobertura.xml
        retention-days: 30
        if-no-files-found: warn
    
    - name: Publish Test Results
      uses: dorny/test-reporter@v1
      if: success() || failure()
      with:
        name: Test Results
        path: 'src/**/TestResults/**/*.trx'
        reporter: dotnet-trx
        fail-on-error: true
        fail-on-empty: true
